export {
  AnimeSeason as PrismaAnimeSeason,
  AnimeType as PrismaAnimeType,
  AnimeStatus as PrismaAnimeStatus,
  EntryStatus as PrismaEntryStatus,
  Method as PrismaMethod,
  Gender as PrismaGender,
  Visibility as PrismaVisibility,
} from '@prisma/client';
